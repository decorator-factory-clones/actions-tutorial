name: banana
on:
  push:
    branches:
      - master

  workflow_dispatch:

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: what does parcel install hmm...
        run: |
          npm i parcel@2
          npm list --depth=69

